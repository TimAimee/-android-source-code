<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" >

    <View
        android:id="@+id/footer_spacer"
        android:layout_width="fill_parent"
        android:layout_height="138.0dip" />

    <TextView
        android:id="@+id/force_refresh"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/footer_spacer"
        android:layout_alignTop="@id/footer_spacer"
        android:gravity="center_horizontal"
        android:paddingTop="46.0dip"
        android:shadowColor="@color/white"
        android:shadowDx="0.0"
        android:shadowDy="2.0"
        android:shadowRadius="0.1"
        android:text="正在刷新"
        android:textColor="@color/feed_subtext"
        android:textSize="16.0dip"
        android:textStyle="bold"
        android:visibility="gone" />

    <ProgressBar
        android:id="@+id/progressbar"
        style="@android:style/Widget.ProgressBar.Large.Inverse"
        android:layout_width="33.0dip"
        android:layout_height="33.0dip"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="46.0dip"
        android:visibility="visible" />

</RelativeLayout>